import XCTest
@testable import SimplexNoiseFilter

final class SimplexNoiseFilterTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimplexNoiseFilter().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
